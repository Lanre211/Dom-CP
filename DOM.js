let iconElement = document.getElementById('iconHeart');

function changeIconColor() {
  iconElement.style.color = 'red';

}

// Attach click event to the icon

iconElement.addEventListener('click', changeIconColor);
  
let iconElemente = document.getElementById('iconHeart1');
function changeIconColorr() {
  iconElemente.style.color = 'red';

}

  // Attach click event to the icon
  iconElemente.addEventListener('click', changeIconColorr);